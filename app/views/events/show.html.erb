<header class="d-flex justify-content-between">
  <div>
    <div class="d-flex">
      <h2><%= @event.name %></h2>
      <% if @event.all_day %>
        <span class="ms-1 h-25 badge bg-secondary">All day</span>
      <% end %>
    </div>
    <span class="badge rounded-pill bg-primary"># <%= @event.category.name %></span>
  </div>
  <div class="ms-2">
    <%= @event.decorated_time_interval %>
  </div>
</header>
<div class="d-flex justify-content-end text-success">
  <span><%= @event.decorated_notify_at %></span>
</div>
<div class="mt-4">
  <div class="text-bg-secondary">
    <%= @event.description %>
  </div>
</div>
