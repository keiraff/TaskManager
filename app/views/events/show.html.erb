<header class="d-flex justify-content-between">
  <div>
    <h2><%= @event.name %></h2>
    <span class="badge rounded-pill bg-primary bg"># <%= @event.category.name %></span>
  </div>
  <div class="ms-2">
    <%= @event.starts_at.to_formatted_s(:long_ordinal) %>
  </div>
</header>
<div class="d-flex justify-content-end text-success">
  <% if @event.notify_at.present? %>
    <span class="me-1">Notification is set on</span>
    <%= @event.notify_at.to_formatted_s(:short) %>
  <% end %>
</div>
<div class="mt-4">
  <div class="text-bg-secondary">
    <%= @event.description %>
  </div>

</div>
