<%= search_form_for @search_result, html: { autocomplete: 'off' } do |f| %>
  <div class="d-flex">
    <%= f.search_field :name_cont, placeholder: "Category Name", class: "form-control me-2" %>
    <%= f.submit 'Search', class: "btn btn-dark" %>
  </div>
<% end %>
